/** 一次请求范围内的工具名双向映射。 */
export interface ToolNameMap {
    /** harness 原名 → 网关合法名（同一原名稳定返回同一结果）。 */
    wire(original: string): string;
    /** 网关回传名 → harness 原名（未登记的原样返回）。 */
    restore(wireName: string): string;
}
/** 创建工具名映射：原名与网关名双向登记，冲突时追加 `_2`/`_3` 后缀。 */
export declare function createToolNameMap(): ToolNameMap;
//# sourceMappingURL=tool-names.d.ts.map