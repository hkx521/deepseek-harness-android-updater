/**
 * Android fallback for the native flock addon.
 *
 * DSH already keeps an in-process write claim for each session. The Android app
 * starts one engine process, so a second kernel lock is not required here.
 */
export async function tryLockExclusive(_fd) {
  return;
}
