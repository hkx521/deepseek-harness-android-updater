# tools/attic —— 已下线的一次性脚本（只归档，不再维护）

这几个脚本是早期手工作业期的产物，**没有任何入口引用它们**（除历史 docs 外零调用方），
而且判据已被更好的工具取代，因此批次86（2026-09-21）按方案 §P3-3 把它们移出主目录。
**没有删除**：需要时 git mv 回 tools/ 即可，历史实现也都还在 git 里。

| 脚本 | 原用途 | 为什么下线 |
|---|---|---|
| regression.sh | 批次19/20 的真机回归清单脚本化（冷启、DocumentsUI 点击、输入链、事务 back/tap） | 判据依赖 su（本机无 root 必失败）；同等覆盖面现在由 tests/*.py 契约 + tools/e2e_*.py 真机脚本承担；清单本身留在 docs/test-flows/regression-checklist.md |
| power-baseline.sh | 批次21 功耗基线测量（每场景默认 900s 长跑） | 项目已改为「快照式验收 ≤60s」（批次81-T4 拍板），长跑脚本与现行纪律冲突 |
| diag-bundle.sh | 一次性诊断包导出（只读收集 logcat/dumpsys 等） | 现场排障已固定为「按 TAG 过滤 logcat + 按需 dumpsys」，没有再打包的步骤 |

纪律提醒：**不要再往 tools/ 根目录加「某批次一次性用」的脚本**——真机验证要留就留成
tools/e2e_<批次>_<主题>.py（有判据、能重跑），临时脚本用完即删。
