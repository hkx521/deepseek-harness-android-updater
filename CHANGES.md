# 更新记录（CHANGES）

本文件只记录**用户可见**的变化。版本号规则：`<App 版本>-dsh<DSH 内核版本>`
（例：`1.9.0-dsh0.1.5rc1` = App 侧 1.9.0、DSH 内核 0.1.5-rc.1）。

- 安装包与校验文件见 [Releases](https://github.com/hkx521/deepseek-harness-android-updater/releases)
- `v1.8.5` 及更早的条目属于**上游开发线**（[woaiys3/deepseek-harness-android-app](https://github.com/woaiys3/deepseek-harness-android-app)），保留以便追溯；本仓库只发布**正式版** APK（上游历史里的「Lite 共存版 / 兼容版」不在本仓库发布）

## v1.9.0-dsh0.1.5rc1（versionCode 28 · 2026-09-22）—— 本仓库首个公开发布

### ✨ 新增

- **定时任务**：管理页可新建 / 编辑 / 启停 / 删除，到点自动执行并回填结果；修掉「到点必失败」的三条 RPC 契约缺陷，失败可见、可重试。
- **多任务管理**：任务互不覆盖，可查看 / 续跟 / 批量清理；任务状态改为**以引擎（dsh）为准**，不再把「引擎仍在跑」误报为失败。
- **托管常驻引擎**：在 root 或 Shizuku 下，引擎以 `setsid` 脱离 App 进程树常驻 `/data/local/tmp/dsh`（**root 优先、Shizuku 兜底**）；App 被杀、清后台、锁屏都不中断，并有看护脚本自愈；面板与设置页显示当前通道。
- **助手材质与动效**：助手面板采用客户端自绘「液态玻璃」（AGSL 棱边折射 + 光谱色散）；入场 / 收起动效对齐系统「灵动胶囊展开」（起点胶囊、钟形速度曲线、轻微过冲回落）。
- **实况窗 / 灵动胶囊**：等待作答 / 需要审批时在系统胶囊显示状态，点一下直达助手面板；定时任务进度与实况互通。
- **一站式权限引导页**：11 项权限集中配置（存储、无障碍、通知使用权、悬浮窗、修改系统设置、使用情况访问、安装未知来源、忽略电池优化、通知、应用启动管理、Shizuku / root 特权），设置里另有「配置手机权限」常驻入口。
- **指令库（Prompt Studio）**：常用指令模板可增删 / 排序，改完即时生效；历史结果支持「重新执行」。
- **高可用模型路由插件** `dsh-model-router`：首字超时（TTFT）哨兵 + 不可恢复错误自动降级到备用 provider。
- **危险操作审批门**：装 / 卸应用、写系统设置、特权 shell 写命令等动作可走确认通知（允许 / 拒绝，超时按拒绝处理）。
- **AI 读取系统通知**：授予「通知使用权」后，AI 可查看最近通知（消息、验证码等）。

### 🔧 优化

- **虚拟屏**：默认机制放宽 —— 只读理解类指令（识别 / 提取 / 翻译 / 总结 / 划选问答）留在主屏直读，其余操作类默认在虚拟屏执行；预览小窗可一键显隐；任务结束自动销毁虚拟屏并回收。
- **助手任务状态实时化**：接上引擎的会话事件流（turn / message 级推送），并支持「▶ 直接执行」一键放行同一会话的授权请求。
- **无障碍工具面扩展**：多指 / 按住 / 拖动 / 分数坐标 / 网格截图 / 状态式虚拟触摸屏；操作成功后自动附带界面摘要，少一次感知调用。
- **退出即清理**：主动「退出」会停止保活服务并回收 App 内引擎；托管引擎走「停止托管引擎」收尾。

### 🐛 修复

- **引擎**：托管模式 DNS 全灭（分组解析失败导致所有 API 请求失败）、托管 home 覆盖用户配置。
- **虚拟屏**：滑动 / 点击坐标错屏、划选提取文字误开黑屏、看图链路 `fsync EINVAL` 失效、主屏读屏被虚拟屏污染。
- **锁屏挂机**：虚拟屏「销毁重建再销毁」导致任务中断；升级 / 收尾路径五处缺陷。
- **呼出与动效**：AI 键呼不出助手、呼出瞬间顶端卡顿与阴影、退回桌面残留长条。
- **启动与运行**：Termux 共存崩溃、慢启动 90s 超时、老 WebView 白屏、内置 curl 证书路径、端口冲突。
- **相册**：外部 DSH 根目录被系统媒体库误判，导致相册出现大量「零分零秒视频」。

### 🔄 变更

- **去 YOYO 化**：不再接管厂商助手 / 语音链路，入口收敛到 AI 键、快捷磁贴、划词、系统分享与助手面板。
- **事件触发器**：能力保留（`android_trigger`），入口按需从设置中撤下。
- `targetSdk` 保持 **28**：≥29 会让私有目录变为 `noexec`，node 二进制无法执行（EACCES）。

### 📦 发布

- 首个公开发布：自签名 APK（versionCode 28 / 内核 `@deepseek-ai/dsh` 0.1.5-rc.1），附 `.sha256` 校验文件；源码见本仓库，安装包见 Releases。

## 历史版本（上游开发线）

| 版本 | 要点 |
|---|---|
| v1.8.5 | 系统分享接入（任意 App「分享 → DeepSeek Harness」直接把内容交给 AI）；runtime 增加 glibc 直跑（零容器）与组装器（sha256 锁定 / 页对齐 / 依赖完备性检查） |
| v1.8.4 | 本地桥接鉴权（随机 token + 请求头校验）；危险操作审批门；AI 读取系统通知；操作后自动附界面摘要；新增 `android_screen_refresh` |
| v1.8.3 / v1.8.2 | 修复：多任务列表残留助手条目、退回桌面残留长条、看图与附件落盘 EACCES、状态查询字段丢失、悬浮窗「引擎在线 false」误判、payload 内 curl 证书路径 |
| v1.8.0 | 内核升级到 DSH 0.1.5-rc.1；新增版本化 Android 兼容层；可重复执行的 payload 准备 / 验证 / 构建 CLI |
| v1.7.5 | Termux 共存修复（内置 node 的 OpenSSL 目录）；无障碍手势引擎（多指 / 按住 / 拖动 / 分数坐标 / 网格截图）；工具输出 schema 修复 |
| v1.7.0 | 无障碍屏幕助手（读屏 / 点击 / 输入 / 返回主页 / 滚动 / 截图理解）；启动失败诊断文件与外部引擎日志 |
| v1.6.5 | AI 工作区（自选外部目录作为文件操作根）；悬浮窗改官方鲸鱼图标并显示引擎与回复状态；内置 curl；老 WebView 兼容检测 |
| v1.6.1 | 修复 Write 工具写文件链路 |
| v1.6.0 | 定时任务增强（重复规则 + 结果通知）；应用使用时长；小鲸鱼悬浮窗与 `android_overlay`；修复识图 / 历史 / 附件 |
| v1.5.5 | 修复慢启动（90s 超时）根因：引擎健康检查只读首页前 4KB，而标题在第 ~13KB 处 |
| v1.5.0 | 端口冲突自动换端口（当时有 bug 的测试版）、ABI 检测、补丁启动自检 |
| v1.4.0 | 特权通道可选（无 root / Shizuku 也能用）、root 优先探测、前台保活、AI 通知 |
| v1.3.3 | 修复相册出现大量「零分零秒视频」（外部 DSH 根目录被系统媒体库误判） |
| v1.3.2 | 修复升级后 UI 不更新（外部运行目录「已有文件不覆盖」策略） |
| v1.3.1 | 移动端 UI 打磨（消息操作条换行、标题栏让位三条杠、正文与输入框宽度、滚动条） |
| v1.3.0 | 修复 v1.2.0 安装即闪退（javac 路径硬编码）；侧栏改造落地 |
| v1.2.0 | 竖屏 UI 改造：左侧竖栏改为左上角三条杠按钮，聊天区全宽 |
| v1.1.1 | 首个稳定基线：启动稳定性（node 运行时 soname 修复 + 保活）与基础移动端可用性；基于原作者 v0.1.0 源码 |
